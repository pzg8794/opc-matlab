;ANS = X1*X2*X3/(X4+X5)

DATA SEGMENT
	X1	DB	FF
	X2	DB	FF
	x3	DB	FF
	X4	DB	FF
	X5	DB	FF
	ANS	DW	?
DATA ENDS

CODE SEGMENT
ASSUME DS:DATA,CS:CODE

XX:	MOV	AX, DATA
	MOV	DS, AX
	MOV	AL, X1
	MOV	BL, X2 
	MUL	BL	;X1*X2
	MOV	BL, X3
	MUL	BL	;X1*X2*X3
	MOV	CX, AX
	MOV	BL, X4  ;
	MOV	CL, X5
	ADD	CL, BL  ;X4+X5
	DIV	CX	;X1*X2*X3/(X4+X5)
	MOV	ANS, AX
	HLT
	CODE ENDS
	END XX