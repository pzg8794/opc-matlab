;Keith Manta
;ANS=X1*X2*X3/(X4+X5)
DATA SEGMENT
	X1	DB	0AAH
	X2	DB	0BBH
	X3	DB	0CCH
	X4	DB	0DDH
	X5	DB	0EEH
	ANS	DW	0
DATA ENDS

CODE SEGMENT
	
	ASSUME CS:CODE,DS:DATA

JOBEL:	MOV	AX,DATA
	MOV	DS,AX
	MOV	AH,00
	MOV	AL,X1
	MOV	BL,X2
	MUL	BL
	MOV	BX,AX
	MOV	AH,0
	MOV	AL,X3
	MUL	BX
	MOV	BL,X4
	MOV	BH,0
	ADD	BL,X5
	ADC	BH
	DIV	BX
	MOV	ANS,AX
	RET

CODE ENDS
END JOBEL

