;STEEVE BISSERETH
;16 BIT SEQUENCE GENERATOR IN AX
;LAB 19 PART B
;TAPS 6,9,12,16
;DETERMINES IF IT PRODUCES
;      65535 STATES

CODE SEGMENT

	ASSUME CS:CODE

SEQ16:	XOR CX,CX
        MOV AX,8000H
AGAIN:	INC CX
	MOV BX,AX
	AND BX,00491H
	OR  BL,BH
	JPE SKIP
	STC
SKIP:	RCR AX,1
	CMP AX,8000H
	JNE AGAIN
	HLT

CODE ENDS
END SEQ16