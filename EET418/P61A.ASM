;PROBLEM #6 1A
;DIAMELA DUVERGE

DATA SEGMENT

X	DB	0E6H
Y	DW	270EH
Z	DW	2 DUP(?)

DATA ENDS

CODE SEGMENT

ASSUME DS: DATA, CS:CODE
XYZ: 	MOV	AL,X ; MOVE BYTE ONE TO AX
	MUL	Y
	SUB	AH,AH
	MUL	Y
	MOV	BX, OFFSET Z
	MOV	[BX],AX
	MOV	[BX]+2,DX
	HLT	
CODE ENDS
END XYZ
END
